apiVersion: "v1"
kind: "ImageStream"
metadata:
  name: "consulta-vertx-service"
spec:
  dockerImageRepository: "andres/consulta-vertx-service"
  tags:
  - annotations: null
    generation: 1
    importPolicy:
      insecure: true
    name: latest